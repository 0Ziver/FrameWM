using Frame.Configs;

namespace Frame.Commands
{
    public class SwitchLayout : ICommand<string>
    {
        public void Execute(string arg)
        {
            /*switch (arg)
            {
                case Layout.GRID:
                    break;
                case Layout.FLEX:
                    break;
                case Layout.ROW:
                    break;
                case Layout.COLLUM:
                    break;
            }*/
        }
    }
}
